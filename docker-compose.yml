services:
  binance-market-monitor:
    build: .
    environment:
      BMM_BIND_HOST: 127.0.0.1
    ports:
      - "127.0.0.1:8000:8000"
    command: ["uv", "run", "binance-market-monitor", "monitor", "--config", "configs/config.example.yaml", "--serve-api"]
